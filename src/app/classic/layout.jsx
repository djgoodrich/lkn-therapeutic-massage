export const metadata = {
  title: 'LKN Therapeutic Massage | Classic Site',
  alternates: { canonical: '/' },
  robots: { index: false, follow: true },
};

export default function ClassicLayout({ children }) {
  return children;
}
